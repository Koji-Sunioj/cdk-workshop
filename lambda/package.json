{
  "name": "lambda",
  "version": "1.0.0",
  "description": "modules for lambda",
  "main": "signup.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "koji",
  "license": "ISC",
  "dependencies": {
    "aws-jwt-verify": "^3.3.0"
  }
}
